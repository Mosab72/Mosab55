# 📋 تعليمات تحويل البيانات إلى JavaScript

## نظرة عامة

هذا الملف يشرح كيفية تحويل بيانات العقود من صيغة الجدول (Excel/CSV) إلى ملف JavaScript يعمل مع النظام.

## 🔄 خطوات التحويل

### الطريقة الأولى: يدوياً (للبيانات القليلة)

1. افتح ملف `data-template.js`
2. انسخ القالب التالي لكل عقد:

```javascript
{
    id: [رقم تسلسلي],
    university: "[اسم الجامعة]",
    college: "[اسم الكلية]",
    program: "[اسم البرنامج/التخصص]",
    degree: "[الدرجة العلمية]",
    contractStatus: "تحت الإجراء",
    contractStart: "[MM/DD/YY]",
    contractEnd: "[MM/DD/YY]",
    documentStatus: "[حالة التسليم]",
    documentReceived: "[التاريخ أو فارغ]",
    completionRate: "[نسبة الإنجاز]",
    updatedDocumentsReceived: "[التاريخ أو فارغ]",
    scheduledVerification: "[التاريخ أو فارغ]",
    scheduledVisit: "[التاريخ أو فارغ]",
    visitStatus: "[حالة الزيارة أو فارغ]",
    department: "[الإدارة المختصة]",
    notes: "[ملاحظات أو فارغ]"
},
```

3. املأ البيانات من الجدول الأصلي
4. احفظ الملف باسم `data.js`

### الطريقة الثانية: باستخدام Excel/CSV (موصى بها)

#### الخطوة 1: تحضير البيانات في Excel

قم بترتيب الأعمدة كالتالي:

| # | العمود | اسم الحقل في JavaScript |
|---|--------|------------------------|
| 1 | تفاصيل العقد | program |
| 2 | اتباع شروط الاعتماد | documentAccreditanceStatus |
| 3 | تاريخ استلام الوثائق | documentReceived |
| 4 | نسبة الإنجاز | completionRate |
| 5 | تاريخ استلام الوثائق المحدثة | updatedDocumentsReceived |
| 6 | التاريخ المجدول لزيارة التحقق | scheduledVerification |
| 7 | اتباع شروط التاريخ المجدول | visitScheduleStatus |
| 8 | التاريخ المجدول لزيارة المراجعين | scheduledVisit |
| 9 | الإدارة المختصة | department |
| 10 | الجامعة / الكلية | university |
| 11 | الدرجة العلمية | degree |
| 12 | حالة العقد | contractStatus |
| 13 | بداية سريان العقد | contractStart |
| 14 | انتهاء سريان العقد | contractEnd |

#### الخطوة 2: استخراج البيانات

استخدم أداة Python البسيطة التالية:

```python
import pandas as pd
import json

# قراءة ملف Excel
df = pd.read_excel('contracts_data.xlsx')

# تحويل إلى قاموس
contracts = []
for index, row in df.iterrows():
    contract = {
        'id': index + 1,
        'university': str(row['الجامعة / الكلية']).split('،')[1].strip() if '،' in str(row['الجامعة / الكلية']) else str(row['الجامعة / الكلية']),
        'college': str(row['الجامعة / الكلية']).split('،')[0].strip() if '،' in str(row['الجامعة / الكلية']) else '',
        'program': str(row['تفاصيل العقد']).split('،')[1].strip() if '،' in str(row['تفاصيل العقد']) else str(row['تفاصيل العقد']),
        'degree': str(row['الدرجة العلمية']),
        'contractStatus': str(row['حالة العقد']),
        'contractStart': str(row['بداية سريان العقد']),
        'contractEnd': str(row['انتهاء سريان العقد']),
        'documentStatus': str(row['اتباع شروط الاعتماد']),
        'documentReceived': str(row['تاريخ استلام الوثائق']) if pd.notna(row['تاريخ استلام الوثائق']) else '',
        'completionRate': str(row['نسبة الإنجاز']),
        'updatedDocumentsReceived': str(row['تاريخ استلام الوثائق المحدثة']) if pd.notna(row['تاريخ استلام الوثائق المحدثة']) else '',
        'scheduledVerification': str(row['التاريخ المجدول لزيارة التحقق']) if pd.notna(row['التاريخ المجدول لزيارة التحقق']) else '',
        'scheduledVisit': str(row['التاريخ المجدول لزيارة المراجعين']) if pd.notna(row['التاريخ المجدول لزيارة المراجعين']) else '',
        'visitStatus': str(row['اتباع شروط التاريخ المجدول']) if pd.notna(row['اتباع شروط التاريخ المجدول']) else '',
        'department': str(row['الإدارة المختصة']),
        'notes': ''
    }
    contracts.append(contract)

# تصدير إلى JavaScript
with open('data.js', 'w', encoding='utf-8') as f:
    f.write('// ملف البيانات - جميع العقود\n')
    f.write('const contractsData = ')
    f.write(json.dumps(contracts, ensure_ascii=False, indent=2))
    f.write(';')

print(f'✅ تم تحويل {len(contracts)} عقد بنجاح!')
```

#### الخطوة 3: تشغيل السكريبت

```bash
# تثبيت المكتبات المطلوبة
pip install pandas openpyxl

# تشغيل السكريبت
python convert_data.py
```

### الطريقة الثالثة: استخدام أداة تحويل أونلاين

1. قم بزيارة أحد المواقع التالية:
   - https://www.convertcsv.com/csv-to-json.htm
   - https://csvjson.com/csv2json

2. قم برفع ملف CSV أو انسخ البيانات

3. اختر خيارات التحويل:
   - Format: Array of Objects
   - Delimiter: Comma
   - Encoding: UTF-8

4. احصل على النتيجة بصيغة JSON

5. أضف الأكواد التالية في البداية والنهاية:

```javascript
// البداية
const contractsData = 

// النهاية
;
```

## 🔍 التحقق من البيانات

بعد التحويل، تأكد من:

### 1. الحقول المطلوبة

كل عقد يجب أن يحتوي على:
- ✅ `id`: رقم فريد
- ✅ `university`: اسم الجامعة
- ✅ `program`: اسم البرنامج
- ✅ `degree`: الدرجة العلمية
- ✅ `contractStatus`: حالة العقد
- ✅ `contractStart`: تاريخ البداية
- ✅ `contractEnd`: تاريخ النهاية
- ✅ `department`: الإدارة المختصة

### 2. صيغة التواريخ

يجب أن تكون التواريخ بصيغة: `MM/DD/YY`

أمثلة صحيحة:
- ✅ `01/23/25` (23 يناير 2025)
- ✅ `03/07/24` (7 مارس 2024)
- ✅ `12/31/26` (31 ديسمبر 2026)

### 3. الدرجات العلمية المدعومة

- بكالوريوس
- ماجستير
- دكتوراه
- دبلوم
- ماجستير تنفيذي

### 4. حالات التسليم

- تم التسليم
- تم التسليم متأخر
- لم يتم التسليم

### 5. الإدارات المختصة

- إدارة برامج العلوم الهندسية وعلوم الحاسب
- إدارة برامج العلوم الصحية
- إدارة برامج العلوم الإنسانية والتربوية
- إدارة برامج العلوم الإسلامية والعربية
- إدارة برامج التخصصات العلمية

## 🧪 اختبار البيانات

بعد إنشاء ملف `data.js`:

1. افتح ملف `index.html` في المتصفح
2. افتح console المتصفح (F12)
3. تأكد من ظهور الرسالة: `✅ تم تحميل XXX عقد بنجاح`
4. تحقق من الإحصائيات في لوحة التحكم
5. اختبر البحث والتصفية

## ❗ مشاكل شائعة وحلولها

### المشكلة 1: أخطاء في الصيغة (Syntax Errors)

**الحل:**
- تأكد من وجود فاصلة بين كل عقد وآخر
- تأكد من إغلاق الأقواس بشكل صحيح `}`
- استخدم validator عبر الإنترنت: https://jsonlint.com/

### المشكلة 2: الأحرف العربية لا تظهر

**الحل:**
- احفظ الملف بترميز UTF-8
- في VS Code: File > Save with Encoding > UTF-8

### المشكلة 3: التواريخ لا تعمل بشكل صحيح

**الحل:**
- تأكد من الصيغة: `MM/DD/YY`
- استخدم أرقام بخانتين للشهر واليوم (01 بدلاً من 1)

### المشكلة 4: لا تظهر أي بيانات

**الحل:**
- تأكد من اسم الملف: يجب أن يكون `data.js` بالضبط
- تأكد من أن الملف في نفس المجلد مع `index.html`
- تحقق من console المتصفح للأخطاء

## 📞 الدعم

إذا واجهت مشاكل:
1. راجع ملف `data-template.js` للأمثلة
2. تحقق من console المتصفح
3. استخدم أدوات التحقق من JSON أونلاين
4. راجع ملف `README.md` للمزيد من المعلومات

## ✅ قائمة التحقق النهائية

قبل نشر النظام، تأكد من:

- [ ] جميع العقود موجودة (445 عقد)
- [ ] جميع الحقول المطلوبة مملوءة
- [ ] التواريخ بصيغة صحيحة
- [ ] لا توجد أخطاء في console المتصفح
- [ ] الإحصائيات تظهر بشكل صحيح
- [ ] البحث يعمل بشكل سليم
- [ ] جميع التبويبات تعمل
- [ ] النظام متجاوب على الهواتف

---

**ملاحظة مهمة**: احتفظ بنسخة احتياطية من البيانات الأصلية دائماً!
